server:
  port: 8084

spring:
  application:
    name: product-catalog-service
  ##Config server is enabled
  cloud:
    config:
      failFast: true
      enabled: true
      uri: http://localhost:8888/

## Configuring info endpoint
info:
  appName: ${spring.application.name}
  description: Spring Product Catalog Service Application
  version: 1.0.0